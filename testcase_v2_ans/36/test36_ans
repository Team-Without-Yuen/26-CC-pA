#RESPONSE 1
已成功載入設計檔案 testcase/test36/test36.v（module top），共有 141 個 primary input 訊號、107 個 primary output 訊號、12428 個邏輯閘。
#END 1
#RESPONSE 2
閘極數量統計：NOT=1241, OR=789, NOR=2429, NAND=5906, XNOR=1272, XOR=79, BUF=22, DFF=690, AND=0，總計 12428 個閘。
#END 2
#RESPONSE 3
No：n10239 不是 primary input 與 primary output 之間的割點；將 n10239 從電路中移除（切斷）後，仍存在其他路徑使某些 primary input 可以到達 primary output。
#END 3
#RESPONSE 4
目前設計與原始載入的網表功能完全等效（尚未做任何結構修改），以 200 組隨機輸入向量對全部 107 個 primary output 進行模擬比對，結果完全一致、無任何差異。
#END 4
#RESPONSE 5
此設計中不存在任何 AND (and) 閘（統計數量為 0），因此沒有任何 AND 閘含有常數 0 輸入。
#END 5
#RESPONSE 6
由於設計中沒有 AND 閘，沒有可簡化的對象，故未執行任何簡化動作，設計維持不變。
#END 6
#RESPONSE 7
0 個：因設計中無 AND 閘、也無帶常數 0 輸入的 AND 閘可供消除，被消除的 AND 閘數量為 0。
#END 7
#RESPONSE 8
g0 是一個 NOT 閘：not g0(n10231, n10232)，輸出接腳(output)=n10231，輸入接腳(input)=n10232。
#END 8
#RESPONSE 9
從任一 primary input 到任一 DFF 的 D 接腳，最大邏輯深度為 122 層（在正反器 g2837 的 D pin 處達到）。
#END 9
#RESPONSE 10
是（對稱，但屬平凡情形）：n11 的實際扇入支持集合僅為 {n0, n1, n2, n8[0], n8[1], n8[2]}，n3 與 n9[0] 皆不在 n11 的扇入錐中，經對支持集合 64 種輸入組合窮舉模擬，交換 n3 與 n9[0] 對 n11 的值完全沒有影響，故 n11 相對於 n3 與 n9[0] 是對稱的。
#END 10
#RESPONSE 11
由時脈 n0 驅動的正反器共有 690 個，即設計中全部 690 個 DFF（g2432 至最後一個 DFF 實例）皆由 n0 驅動，未發現由其他時脈訊號驅動的 DFF。
#END 11
#RESPONSE 12
n0 與 n1 並列扇出最高的 primary input，兩者扇出皆為 690（其餘 PI 扇出遠低於此，例如 n10[2]、n8[1]、n8[0] 僅為 5）。
#END 12
#RESPONSE 13
已完成緩衝器插入：對所有原始扇出超過 16 的 168 個訊號（不含常數 1'b1 字面值）採用鏈狀(spine)緩衝樹結構，以達到理論最小新增閘數，共新增 285 個 BUF 閘，最終設計總閘數為 12713 個（原 12428 個 + 新增 285 個 BUF）；並以 200 組隨機向量驗證插入後功能與原設計完全等效，無任何差異。
#END 13
#RESPONSE 14
插入緩衝器後，n1 的最大直接扇出為 16（已達到扇出上限，不再超過限制）。
#END 14
#RESPONSE 15
插入緩衝器後，n0 的扇出為 16。n0 直接驅動的閘為：15 個原始 DFF 的 CK 接腳（g2432、g2433、g2434、g2435、g2436、g2437、g2438、g2439、g2440、g2441、g2442、g2443、g2444、g2445、g2446）以及 1 個新插入的緩衝器 buf_ins_93（其餘原本由 n0 驅動的 DFF 則透過此緩衝器鏈間接驅動）。
#END 15
#RESPONSE 16
已將目前設計（含緩衝器插入後結果，共 12713 個閘、141 個 PI、107 個 PO）寫出至輸出檔案 test36_out.v。
#END 16
