This is the beginning of a new testcase. The case name is test089.
Please load the design from the file test089.v located in the directory testcase/test089/.
This design implements y1 = (a + b) * c; y2 = (a + b) * d, which currently recomputes the following shared value rather than sharing it: a + b (feeds both multipliers, which themselves multiply by different operands c/d and are NOT shared). Restructure the netlist so each shared computation is computed once and reused by every consumer instead of being duplicated. Ensure the design functionality does not change. The cost function is the total gate count of the final design; smaller is better.
What is the total gate count of the design now?
Please write the current design to the output file test089_out.v.
