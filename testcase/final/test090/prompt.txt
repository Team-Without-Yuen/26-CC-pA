This is the beginning of a new testcase. The case name is test090.
Please load the design from the file test090.v located in the directory testcase/test090/.
This design implements y1 = a * b + c * d; y2 = a * b - c * d, which currently recomputes the following shared value rather than sharing it: a * b and c * d (each product feeds both the sum and the difference). Restructure the netlist so each shared computation is computed once and reused by every consumer instead of being duplicated. Ensure the design functionality does not change. The cost function is the total gate count of the final design; smaller is better.
What is the total gate count of the design now?
Please write the current design to the output file test090_out.v.
