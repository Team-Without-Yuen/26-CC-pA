This is the beginning of a new testcase. The case name is test086.
Please load the design from the file test086.v located in the directory testcase/test086/.
This design implements p = a * b; q = (a * b) + (a * b), which currently recomputes the following shared value rather than sharing it: a * b (computed three times total across p and the two operands of q; all three collapse to one multiplier). Restructure the netlist so each shared computation is computed once and reused by every consumer instead of being duplicated. Ensure the design functionality does not change. The cost function is the total gate count of the final design; smaller is better.
What is the total gate count of the design now?
Please write the current design to the output file test086_out.v.
