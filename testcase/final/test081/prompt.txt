This is the beginning of a new testcase. The case name is test081.
Please load the design from the file test081.v located in the directory testcase/test081/.
This design implements mul = a * b; mac = a * b + c, which currently recomputes the following shared value rather than sharing it: a * b (feeds both mul and mac). Restructure the netlist so each shared computation is computed once and reused by every consumer instead of being duplicated. Ensure the design functionality does not change. The cost function is the total gate count of the final design; smaller is better.
What is the total gate count of the design now?
Please write the current design to the output file test081_out.v.
